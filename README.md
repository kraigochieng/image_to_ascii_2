# Overview

`image_to_ascii_2` is a Python application that converts images into ASCII art. It processes image files and outputs a text-based representation using ASCII characters.

# Features

-   Convert images (JPEG, PNG, etc.) to ASCII art
-   Adjustable output width and character set
-   Supports grayscale and color ASCII output
-   Command-line interface for easy usage

# Installation

1. Clone the repository:

    ```
    git clone https://github.com/yourusername/image_to_ascii_2.git
    cd image_to_ascii_2
    ```

2. Install dependencies:
    ```
    pip install -r requirements.txt
    ```

# Usage

Run the main script with the required arguments:
